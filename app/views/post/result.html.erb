<h1>SEARCH RESULT</h1>
  
<hr>

<% if @resultPost.empty? %>
    <h3><b class="text-warning"><%= @keyword %></b>에 대한 검색 결과가 없습니다.</a></h3>
    <!-- 검색 결과 없으면 카카오(다음) 검색 API 사용, 카카오 검색 결과 보여주기 -->
<% else %>
 <h4><b class="text-warning"><%= @keyword %></b>에 대한 검색결과입니다.</a></h4>
    <table class="table table-hover table-striped">
        <thead>
            <th width="100px">글 번호</th>
            <th>글 제목</th>
            <th width="60px"></th>
        </thead>
        <tbody>
            <% @resultPost.each do |onePost| %>
                <tr>
                    <td><%= onePost.id %></td>
                    <td>
                        <%= link_to onePost.title, "show/#{onePost.id}" %>
                    </td>
                    <td>
                        <%= link_to '', "/edit/#{onePost.id}", class: "glyphicon glyphicon-pencil" %>&nbsp;
                        <%= link_to '', "/destroy/#{onePost.id}", class: "glyphicon glyphicon-remove" %>
                    </td>
                </tr>
            <% end %>
        </tbody>
        <tfoot>
            
        </tfoot>
    </table>
<% end %>

<div class="form-group row">
    <div class="col-md-2 col-md-offset-10">
        <a href="/" class="btn btn-default">목록으로 돌아가기</a>
    </div>
</div>